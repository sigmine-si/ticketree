node_modules/
.next/
dist/
*.tsbuildinfo

.env
.env.local
!.env.example

# 에이전트 워크스페이스 — 영속 데이터, git 관리 대상 아님
workspaces/

# 도그푸딩 — 이 저장소가 워크스페이스로 복제됐을 때 생기는 구현 worktree
work/

.DS_Store

# playwright mcp 산출물
.playwright-mcp/
*.png
