node_modules/
.next/
dist/
*.tsbuildinfo

.env
.env.local
!.env.example

# 에이전트 워크스페이스 — 영속 데이터, git 관리 대상 아님
workspaces/

# 러너가 구현 job에서 만드는 worktree (프로젝트 워크스페이스 하위)
work/

.DS_Store

# playwright mcp 산출물
.playwright-mcp/
*.png

# 로컬 기동 로그 (scripts/deploy.sh)
.logs/
