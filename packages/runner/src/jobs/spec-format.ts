/**
 * 고객 저장소 명세의 파일 형식.
 *
 * 두 job이 명세를 **처음부터** 만든다 — 온보딩(이미 있는 코드에서 뽑아낸다)과
 * 과업내용서(아직 없는 것을 약속한다). 둘이 같은 형식을 써야 하는 이유는
 * 웹의 명세 화면 파서(`packages/web/src/lib/specs.ts`)가 이 형식을 읽기 때문이다.
 * 형식을 두 곳에 적으면 한쪽만 고쳐져 화면이 깨진다.
 *
 * 체크박스 규칙만 두 job이 각자 덧붙인다 — 온보딩은 이미 동작하니 `- [x]`,
 * 과업내용서는 아직 없는 약속이니 `- [ ]`다.
 */
export const SPEC_FILE_FORMAT = `각 기능 명세는 이 구조를 지킨다.

\`\`\`
# 기능 이름

버전: v1.0 · 마지막 변경 <오늘 날짜>
순서: 1

## 이렇게 동작해요

- [x] 클라이언트가 읽을 수 있는 말로 쓴 동작 한 줄

## 알려진 제약

- 지금 안 되는 것

## 구현 규칙 (내부)

- 계산식·조건·경계값. 코드를 다시 뒤져야만 알 수 있는 것을 여기 남긴다.

## 변경 이력

- v1.0 <오늘 날짜> 무엇이 왜 바뀌었나
\`\`\`

- 파일명은 영문 소문자다 (예: \`coupon.md\`).
- 계약(이렇게 동작해요)에는 파일명·함수명·기술 용어를 쓰지 않는다.
  "결제 완료 웹훅에 훅을 겁니다"가 아니라 "결제가 끝나는 시점에 적립됩니다".
- 설계(구현 규칙)에는 코드를 다시 뒤져야만 알 수 있는 것을 남긴다.
  **못 정한 것은 "(미정)"으로 남긴다** — 비워두면 구현 담당이 혼자 정하게 된다.
- \`순서:\`는 클라이언트가 서비스를 겪는 순서대로 매긴다(접수 → 결제 → 배송처럼).
- \`## 알려진 제약\`도 클라이언트 화면에 그대로 나온다. 안 되는 것을 숨기지 않는다 —
  명세는 계약서라 제약이야말로 보여야 하는 쪽이다.`

/**
 * 명세 세 층. 어느 기능도 혼자 소유하지 않는 사실이 흐름 층으로 간다.
 * 같은 사실을 두 곳에 적지 않는 것이 이 구조의 전부다.
 */
export const SPEC_LAYERS = `명세는 세 층이다. **같은 사실을 두 곳에 적지 않는다** —
어긋나면 어느 쪽이 맞는지 알 수 없다. 사실마다 주인을 하나 정하고 나머지는 링크한다.

1. \`specs/product.md\` — 이 서비스가 **무엇을 하는 물건인지**, 왜 만드는지, 누가 쓰는지, 용어.
2. \`specs/overview.md\` — 전체 흐름. 어느 기능도 혼자 소유하지 않는 것만 담는다
   (주요 흐름, 데이터, 외부 연동, 지켜야 할 수준).
3. \`specs/features/*.md\` — 기능별 명세와 그 기능이 소유한 규칙.`
